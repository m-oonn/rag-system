# 项目学习指南

> 回来看这个，10 分钟理解整个项目。

## 一、一个请求的生命周期

这是理解项目最快的方式——跟着一个 RAG 问答请求走一遍：

```
用户提问 "Who created Python?"
        │
        ▼
    chat.py  (API 路由)
        │  认证 + 创建 session
        ▼
    rag_service.py  (RAG 核心)
        │
        ├── memory_service.py   获取对话历史 (短期记忆)
        ├── question_analyzer.py 判断是否多跳问题
        │
        ├── vector_retriever.py  向量检索
        │   ├── embedding/       文本 → 向量
        │   └── vector_db.py     Chroma 搜索 Top-K
        │
        ├── reranker.py          CrossEncoder 精排
        │
        └── llm_client.py        调用 DeepSeek/Ollama 生成回答
                │
                ▼
           返回: {answer, source_documents, session_id}
```

## 二、核心文件速查

### 你改了配置就生效的文件

| 文件 | 作用 | 什么时候改 |
|------|------|-----------|
| `.env` | 所有配置入口 | 换模型、改参数、开关功能 |
| `src/settings.py` | 配置定义 + 默认值 | 加新配置项 |

### 核心链路（面试必问）

| 文件 | 做什么 | 面试怎么说 |
|------|--------|-----------|
| `src/services/rag_service.py` | RAG 主流程：检索→重排→生成 | "我实现了完整的 RAG 管道" |
| `src/llm/llm_client.py` | 三模型客户端 | "支持 Ollama/DeepSeek/Qwen 切换" |
| `src/database/vector_db.py` | Chroma + Milvus 适配器 | "双模式向量库，开发用 Chroma 生产用 Milvus" |
| `src/retrieval/reranker.py` | CrossEncoder 重排序 | "用 CrossEncoder 做精排提升召回精度" |
| `src/embedding/` | 文本向量化 | "支持 sentence-transformers 和 DashScope" |
| `src/services/memory_service.py` | 对话记忆 | "短期用 Redis/内存，长期用向量库" |

### 文档处理链路

| 文件 | 做什么 |
|------|--------|
| `src/processors/multi_doc_parser.py` | 多格式解析入口 |
| `src/processors/pdf_parser.py` | PDF 解析 (PyMuPDF) |
| `src/processors/text_chunker.py` | 文本分割 (LangChain) |

### API 路由（12 个）

| 路由 | 用途 |
|------|------|
| `auth.py` | 注册/登录 |
| `knowledge_base.py` | 知识库 CRUD + 文档上传 |
| `chat.py` | RAG 问答 |
| `evaluation.py` | RAGAS 评测 |
| `assistant.py` | 助手配置 |
| `agent.py` | Agent 配置 |
| `health.py` | 健康检查 |
| `monitor.py` | 队列监控 |
| 其余 | 文件管理/存储/查询 |

## 三、我改了什么（对比原始课程代码）

原始课程代码要求启动 5 个外部服务（Milvus + etcd + MinIO + RabbitMQ + Redis）才能跑。我做了适配层，让它零依赖也能跑：

| 改动 | 文件 | 效果 |
|------|------|------|
| 双模式向量库 | `vector_db.py` | 新增 ChromaStore，保留 MilvusStore |
| 多模型 LLM | `llm_client.py` | 支持 Ollama/DeepSeek/DashScope |
| 多 provider Embedding | `embedding/` | 新增 sentence-transformers |
| 多模式 Reranker | `reranker.py` | 本地 CrossEncoder + DashScope |
| 内存降级 | `memory_service.py` | Redis 不可用→内存字典 |
| Celery 降级 | `knowledge_base.py` | 文档同步处理，不需要消息队列 |
| 路由器可选化 | `main.py` | 缺失依赖时优雅跳过 |
| 单例模式 | `rag_service.py` | 避免重复加载模型 |
| Streamlit 前端 | `streamlit_app.py` | 纯 Python 前端，无需 Node.js |

## 四、配置切换速查

### 换模型（改 .env 一行）

```env
LLM_PROVIDER=ollama       # 免费，需 ollama serve
LLM_PROVIDER=deepseek     # 便宜，需 DEEPSEEK_API_KEY
LLM_PROVIDER=dashscope    # 通义千问，需 DASHSCOPE_API_KEY
```

### 换向量库

```env
VECTOR_STORE=chroma       # 零依赖，本地文件
VECTOR_STORE=milvus       # 需 Docker 启动 Milvus
```

### 开/关重排序

```env
RERANK_PROVIDER=local     # CrossEncoder，精度高但慢
RERANK_PROVIDER=none      # 跳过，速度快
```

### 切企业模式

```env
ENABLE_CELERY=true        # 异步文档处理
ENABLE_MINIO=true         # MinIO 对象存储
VECTOR_STORE=milvus       # Milvus 向量库
```

## 五、面试话术模板

**"介绍一下你的项目"** (3 分钟版)

> 我做了一个 RAG 知识库问答系统。技术栈是 FastAPI + LangChain + Chroma + DeepSeek。
>
> 核心流程是：用户上传文档后，系统自动解析、分块、向量化存入 Chroma。提问时先向量检索 Top-K 文档，再用 CrossEncoder 重排序，最后把上下文和问题一起发给 LLM 生成带引用的回答。
>
> 最大的亮点是我做了双模式架构：开发时用 Chroma + Ollama 零依赖直接跑，生产环境可以无缝切换到 Milvus + Qwen-Max + Redis + Celery 的企业级方案，只改配置文件不碰代码。
>
> 另外我还集成了 RAGAS 评测框架，可以自动生成测试数据集，量化评估检索效果。

**"遇到的最大技术挑战？"**

> 原始课程代码依赖 5 个外部服务，本地根本跑不起来。我分析了核心链路，用适配器模式给向量库、LLM、Embedding、Reranker 都加了轻量级实现，同时保留企业级方案。这样既保证了开发效率，又保留了扩展性。

## 六、测试命令

```bash
# 启动后端
cd ragPdfSystem
python -m uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload

# 浏览器打开 API 文档
http://localhost:8000/docs

# Streamlit 前端
streamlit run streamlit_app.py

# 查看 SQLite 数据
python -c "import sqlite3; c=sqlite3.connect('rag_system.db'); c.row_factory=sqlite3.Row; [print(dict(r)) for r in c.execute('SELECT * FROM knowledge_documents').fetchall()]"
```
