# 面试背诵卡 — 25 题 + 项目讲解模板

> 每道题先把**加粗的一句话**背下来，再展开细节。

## RAG 方向（10题）

### 1. RAG 的完整流程？
**离线索引：文档→解析→分块→Embedding→向量库。在线查询：问题→Embedding→检索→重排序→拼接 Prompt→LLM 生成→带引用回答。**
- 离线：PDF/DOCX→文本→RecursiveCharacterTextSplitter→sentence-transformers→Chroma
- 在线：query embedding→Chroma.search(top_k)→CrossEncoder rerank→prompt template→LLM

### 2. Embedding 是什么？为什么能表示语义？
**Embedding 把文本映射到高维向量空间，语义相近的文本向量距离近。**
- 训练目标：相似文本拉近，不相似文本推远（对比学习）
- 我的项目用 BGE-small-zh-v1.5（中文优化，512维）或 all-MiniLM-L6-v2（英文，384维）

### 3. 向量数据库和传统数据库的区别？
**传统数据库精确匹配（WHERE name='Python'），向量数据库近似匹配（找语义最相似的 Top-K）。**
- 向量库用 ANN（近似最近邻）算法：HNSW、IVF
- Chroma 内置 HNSW，余弦相似度
- 为什么选 Chroma：嵌入式、零部署、Python 原生、支持持久化

### 4. 文本分割的策略？chunk_size 怎么选？
**我用 RecursiveCharacterTextSplitter，chunk_size=1000，overlap=200。中文优化分隔符：段落→换行→句号。**
- chunk 太大会稀释语义，太小会切断上下文
- 500-1000 字符是经验值，overlap 防止关键信息被切断
- 面试加分：提到可以根据文档类型动态调整（PDF 用小 chunk，Markdown 用大 chunk）

### 5. 混合检索是什么？为什么比纯向量好？
**BM25（关键词）+ 向量（语义）+ RRF（倒数排名融合）。纯向量对精确关键词（如"Python 3.12"）匹配差，BM25 补这个短板。**
- BM25 擅长精确匹配：产品型号、版本号、专有名词
- 向量擅长语义匹配：同义词、改写、模糊查询
- RRF 公式：score = Σ 1/(k+rank_i)，k=60
- 我在项目里用 ENABLE_HYBRID_SEARCH=True 控制开关

### 6. Reranker 的作用？Bi-encoder vs Cross-encoder？
**Reranker 对粗排结果做精排。Bi-encoder 快但精度低（分开编码），Cross-encoder 慢但精度高（联合编码）。**
- Bi-encoder：query 和 doc 独立编码，可以预计算 doc 向量
- Cross-encoder：query 和 doc 一起输入，计算相似度分数
- 我的流程：Bi-encoder 粗排 Top-20 → Cross-encoder 精排 Top-5
- 模型：ms-marco-MiniLM-L-6-v2 (384维，轻量)

### 7. 怎么评估 RAG 效果？用什么指标？
**用 RAGAS 框架，核心指标：Faithfulness（忠实度）、Answer Relevance（答案相关性）、Context Precision（上下文精确度）。**
- 我的项目里 /api/v1/evaluations/ 端点可以跑自动化评测
- 自动生成 QA 对 → 执行 RAG → 计算指标 → 生成报告

### 8. RAG vs 微调，什么场景用什么？
**需要实时更新知识 → RAG。需要固定风格或领域术语 → 微调。两者可以结合。**
- RAG：知识外挂，随时更新，可解释（有引用来源）
- 微调：改变模型行为，降低推理成本，但更新知识需重新训练
- 企业场景通常 RAG 先上，效果好就不再微调

### 9. 怎么防止 LLM 编造答案？
**Prompt 里加约束"如无相关信息请明确说明" + 引用来源强制 + RAGAS Faithfulness 监控。**
- Prompt 模板里写死："如上下文无相关信息，明确说明'根据提供的信息无法回答'"
- 每个回答标注引用段落
- 评测时专门看 Faithfulness 指标

### 10. 你项目中遇到的最大技术挑战？
**原始课程代码依赖 Milvus + Redis + RabbitMQ + MinIO + etcd 五个外部服务，本地跑不起来。我用适配器模式给向量库、LLM、Embedding、Reranker 全做了轻量实现，同时保留企业方案，改配置文件不碰代码。**

## Agent 方向（8题）

### 1. Agent 和普通 LLM 调用有什么区别？
**普通 LLM 调用：一问一答。Agent：LLM + 工具 + 规划 + 记忆，能自主决策、多步执行。**
- Agent = LLM（大脑）+ Tools（手脚）+ Memory（记忆）+ Planning（规划）
- 比如"帮我分析这份销售数据"：Agent 先读文件→写 pandas 代码→执行→画图→写报告

### 2. Function Calling 的原理？
**LLM 输出的不是文本，是 JSON 格式的函数调用指令（函数名+参数），程序执行后把结果送回 LLM 继续生成。**
- 流程：用户输入 → LLM 决定调用哪个工具 → 输出 {name:"calculator", args:{a:1,b:2}} → 程序执行 → 返回结果 → LLM 生成最终回答

### 3. ReAct 模式是什么？
**Reasoning + Acting 交替循环。思考→行动→观察→思考→行动→...直到得出答案。**
- 例如："北京今天多少度？" 
- Thought: 需要查天气 → Action: search("北京天气") → Observation: 25°C → Thought: 有答案了 → Answer: 25°C

### 4. LangGraph 和 LangChain 的关系？
**LangChain 是工具集（LLM调用、文档加载、链），LangGraph 是编排框架（图结构、状态管理、条件路由）。**
- LangChain：帮我调 LLM、加载文档、做 RAG
- LangGraph：帮我画 Agent 工作流图，节点=步骤，边=流转
- 项目一用 LangChain 做 RAG，项目二用 LangGraph 做 Agent

### 5. Agent 的记忆怎么实现？
**短期记忆：对话窗口（最近 N 轮）。长期记忆：向量库存储重要信息，按需检索。**
- 短期：内存字典或 Redis，设置 TTL 过期
- 长期：把用户偏好、关键事实向量化存 Chroma，新对话时检索相关记忆
- 我的项目 memory_service.py 实现了双模式

### 6. 代码执行的安全风险？怎么做沙箱？
**LLM 生成的代码可能删文件、访问网络。用 Docker 沙箱或 subprocess+限制权限。**
- 最低限度：subprocess 执行 + timeout + 禁止 import os/sys/subprocess
- 生产环境：Docker 容器执行，网络隔离，只读文件系统
- 面试时提："我在开发阶段用 subprocess 限制，生产用 Docker 沙箱"

### 7. 你的 Agent 怎么处理执行失败？
**最多重试 3 次，每次把错误信息喂给 LLM 让它修正。3 次仍失败就降级为用户手动处理。**
- 错误自修复循环：执行失败 → 错误信息 + 原任务 → LLM 改写代码 → 重新执行
- 这是项目二的亮点设计

### 8. 你的数据分析 Agent 的工作流程？
**上传 CSV → 用户描述需求 → Agent 分析（选列、选聚合、选图表类型）→ 生成 pandas 代码 → 执行 → 生成图表 → 写 Markdown 报告。**

## 通用技术（7题）

### 1. 为什么选 FastAPI？
**原生异步（async/await）、自动生成 Swagger 文档、Pydantic 数据验证、性能对标 Node.js。**
- 对比 Flask：FastAPI 异步性能好 2-3 倍
- 类型提示驱动开发，减少运行时错误

### 2. 异步编程解决什么问题？
**IO 密集场景（网络请求、数据库查询）不阻塞主线程，提高吞吐量。gather 并发执行多个协程。**
- async/await + FastAPI 可以让一个请求等待 LLM 响应时处理其他请求
- asyncio.gather(task1, task2, task3) 并发执行，等全部完成

### 3. 流式输出（SSE）的原理？
**Server-Sent Events：服务器持续推送数据，客户端通过 EventSource 监听。Content-Type: text/event-stream。**
- 我的项目：/api/v1/chat/stream 端点
- LLM 逐 token 生成→通过 SSE 推送→前端逐字显示（类 ChatGPT 体验）
- 对比 WebSocket：SSE 单向（服务器→客户端），更简单

### 4. Docker 的基本概念？
**Dockerfile 描述环境，Image 是打包好的环境快照，Container 是运行的实例。解决的问题："我机器上能跑，你机器上不行"。**
- Dockerfile → docker build → Image → docker run → Container
- docker-compose.yml 编排多容器（app + Milvus + Redis + ...）
- 我的项目：轻量模式单容器，企业模式多容器

### 5. Prompt Engineering 的核心技巧？
**角色设定 + 任务描述 + 输出格式 + Few-shot 示例 + 约束条件。**
- RAG Prompt 模板：
  - 角色：你是一个知识问答助手
  - 任务：根据提供的文档回答问题
  - 约束：只根据文档回答，不要编造
  - 格式：引用段落编号

### 6. 你对 MCP 协议的了解？
**Model Context Protocol：Anthropic 发布的开放协议，标准化 AI 模型与外部工具/数据源的连接方式。类似"AI 的 USB 接口"。**
- MCP Server 暴露工具（如数据库查询、文件操作）
- MCP Client（如 Claude Desktop）发现并调用这些工具
- 未来趋势：Agent 的标准化工具接口

### 7. 你的项目还有什么改进空间？
**检索层面：加 Query Rewriting（用 LLM 改写用户问题提升检索质量）、Self-RAG（检索后自我评估是否需要更多信息）。工程层面：加 Prometheus 监控、日志 ELK、压力测试。**

---

## 项目讲解模板

### 3 分钟版（面试标准）

> **背景（30秒）**
> 我做了一个企业级 RAG 知识库问答系统。用户上传 PDF/Word 等文档，系统自动解析、向量化，然后用户可以提问，系统从文档中检索相关段落，结合 LLM 生成带引用的回答。
>
> **技术架构（60秒）**
> 技术栈是 FastAPI + LangChain + Chroma + DeepSeek。后端有 12 个 API 路由，前端有 Streamlit 和 Vue 3 两个版本。
> 最大的亮点是我做了双模式架构：开发时用 Chroma + sentence-transformers + DeepSeek，零外部依赖直接跑；生产环境可以切换到 Milvus + DashScope + Redis + Celery 企业方案，只改配置文件。
> 检索上我做了混合检索——BM25 关键词 + 向量语义 + RRF 融合，解决了纯向量对精确关键词匹配差的短板。还有 CrossEncoder 重排序，粗排后再精排。
>
> **核心难点（60秒）**
> 最大的挑战是原始课程代码依赖 5 个外部服务，本地根本跑不起来。我花了时间用适配器模式给向量库、LLM、Embedding、Reranker 都加了轻量级实现，同时保留企业级方案。
> 还踩了一个坑：切换中文 Embedding 模型（BGE）后，向量维度从 384 变成 512，Chroma 集合维度不匹配。我改了 ChromaStore 让它不再绑定固定维度，改模型不需要重建索引。
>
> **成果（30秒）**
> 项目已在 GitHub 开源，有完整 README、Docker 部署、API 文档、自动化测试。支持三种 LLM（Ollama/DeepSeek/Qwen）一键切换。

---

## 复述自检（对着镜子练，每个能讲 1 分钟就合格）

- [ ] RAG 完整流程画出来
- [ ] 混合检索讲清楚为什么比纯向量好
- [ ] Embedding 是什么用类比解释（"把语义变成坐标"）
- [ ] Bi-encoder vs Cross-encoder 区别
- [ ] RAG vs 微调使用场景
- [ ] Agent 和普通 LLM 调用的区别
- [ ] ReAct 模式用例子讲一遍
- [ ] 你的项目最大的技术挑战和解决方案
- [x] 3 分钟项目讲解
