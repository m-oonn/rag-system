# RAG 知识库问答系统 — 全方位学习文档

> 覆盖项目全部技术点，从零基础到面试通关。建议学习顺序：**架构概览 → 逐层深入 → 练习题巩固 → 面试题冲刺**。

---

## 目录

1. [系统架构全景图](#一系统架构全景图)
2. [技术栈逐项精讲](#二技术栈逐项精讲)
   - FastAPI 后端框架
   - LangChain 文档处理
   - Embedding 文本向量化
   - ChromaDB 向量数据库
   - 混合检索：BM25 + 向量 + RRF
   - CrossEncoder 重排序
   - LLM 多模型客户端
   - SSE 流式输出
   - SQLAlchemy + SQLite
   - JWT 认证
   - Streamlit 前端
   - Docker 容器化
3. [项目核心链路逐行解析](#三项目核心链路逐行解析)
4. [综合练习题（30 题）](#四综合练习题)
5. [面试模拟题（40 题）](#五面试模拟题)
6. [参考答案](#六参考答案)

---

## 一、系统架构全景图

### 1.1 一个 RAG 问答请求的完整生命周期

```
用户浏览器                    FastAPI 服务器                   外部服务
    │                              │                              │
    │  POST /api/v1/chat/          │                              │
    │  {"query": "Python谁创建的?"} │                              │
    │ ─────────────────────────>   │                              │
    │                              │  1. JWT 认证 (auth.py)       │
    │                              │  2. 解析请求体 (Pydantic)    │
    │                              │                              │
    │                              │  3. RAGService.query()       │
    │                              │     ├─ MemoryService         │
    │                              │     │  └─ 获取对话历史       │
    │                              │     ├─ QuestionAnalyzer      │
    │                              │     │  └─ 判断是否多跳问题   │
    │                              │     ├─ HybridRetriever       │
    │                              │     │  ├─ BM25 关键词检索    │
    │                              │     │  ├─ 向量语义检索       │
    │                              │     │  └─ RRF 融合排序       │──→ ChromaDB
    │                              │     ├─ CrossEncoder Reranker │
    │                              │     │  └─ 对 Top-K 精排      │
    │                              │     └─ LLMClient             │
    │                              │        └─ 拼接 Prompt 生成   │──→ DeepSeek/Ollama
    │                              │                              │
    │  {"answer": "...",           │  4. 保存到 SQLite            │
    │   "source_documents": [...]} │  5. 更新对话记忆             │
    │ <─────────────────────────   │                              │
```

### 1.2 文档上传链路

```
用户上传 PDF/DOCX
    │
    ▼
knowledge_base.py  ─→  保存文件到本地
    │
    ▼
MultiDocParser  ─→  解析文档内容 (PyMuPDF/python-docx)
    │
    ▼
TextChunker  ─→  RecursiveCharacterTextSplitter 分割成 chunks
    │
    ▼
SentenceTransformersEmbedding  ─→  每个 chunk → 512维向量
    │
    ▼
ChromaStore.insert()  ─→  向量 + 原文 + 元数据 存入 ChromaDB
    │
    ▼
SQLite  ─→  记录文档状态 (doc_id, status=2 Completed)
```

### 1.3 目录结构与职责

```
ragPdfSystem/
├── src/
│   ├── main.py                  # FastAPI 入口，组装所有路由
│   ├── settings.py              # 全局配置（从 .env 读取）
│   │
│   ├── api/routers/             # API 路由层（12个）
│   │   ├── auth.py              #   注册/登录 (JWT)
│   │   ├── knowledge_base.py    #   知识库 CRUD + 文档上传
│   │   ├── chat.py              #   RAG 问答 + SSE 流式
│   │   ├── evaluation.py        #   RAGAS 自动评测
│   │   ├── assistant.py         #   助手配置
│   │   ├── agent.py             #   Agent 配置
│   │   └── ...                  #   health/monitor/storage/query
│   │
│   ├── services/                # 业务逻辑层
│   │   ├── rag_service.py       #   RAG 核心管道
│   │   ├── memory_service.py    #   对话记忆
│   │   ├── evaluator.py         #   评测引擎
│   │   ├── qa_generator.py      #   QA 对自动生成
│   │   └── question_analyzer.py #   多跳问题分析
│   │
│   ├── llm/                     # LLM 层
│   │   └── llm_client.py        #   三模型客户端 (Ollama/DeepSeek/Qwen)
│   │
│   ├── embedding/               # Embedding 层
│   │   ├── __init__.py          #   工厂函数 + 单例
│   │   ├── base.py              #   抽象基类
│   │   ├── sentence_embedding.py #  本地 BGE/all-MiniLM
│   │   └── dashscope_embedding.py # 阿里云 Embedding
│   │
│   ├── retrieval/               # 检索层
│   │   ├── vector_retriever.py  #   向量检索
│   │   ├── hybrid_retriever.py  #   混合检索 (BM25+向量+RRF)
│   │   ├── reranker.py          #   重排序 (CrossEncoder/DashScope)
│   │   └── base_retriever.py    #   检索基类
│   │
│   ├── database/                # 数据层
│   │   ├── models.py            #   SQLAlchemy 模型 (13张表)
│   │   ├── sql_session.py       #   数据库连接
│   │   └── vector_db.py         #   Chroma + Milvus 适配器
│   │
│   ├── processors/              # 文档处理层
│   │   ├── multi_doc_parser.py  #   多格式解析入口
│   │   ├── pdf_parser.py        #   PDF 解析
│   │   └── text_chunker.py      #   文本分割
│   │
│   └── utils/                   # 工具
│       ├── logger.py            #   Loguru 日志
│       ├── security.py          #   JWT 工具
│       └── preview_utils.py     #   文件预览
│
├── streamlit_app.py             # Streamlit 前端
├── demo.py                      # 一键 Demo
├── tests/test_api.py            # Pytest 测试 (12项)
├── docker/                      # Docker 配置
├── frontend/                    # Vue 3 前端
├── 项目学习指南.md
├── 面试背诵卡.md
└── .env                         # 配置文件
```

---

## 二、技术栈逐项精讲

### 2.1 FastAPI 后端框架

**是什么：** Python 的现代异步 Web 框架，基于 Starlette（异步）和 Pydantic（数据验证）。性能对标 Node.js 和 Go。

**为什么选它：**
- 原生 async/await 支持，LLM 调用是 IO 密集场景，异步不阻塞
- 自动生成 Swagger 文档（`/docs`），开发调试方便
- Pydantic 自动验证请求/响应，减少运行时错误
- 依赖注入系统（Depends），优雅管理数据库连接和认证

**核心概念：**

```python
# 1. 路由定义
@app.get("/items/{item_id}")        # 路径参数
async def read_item(item_id: int, q: str = None):  # 查询参数
    return {"item_id": item_id, "q": q}

# 2. 请求体 (Pydantic Model)
from pydantic import BaseModel
class Item(BaseModel):
    name: str
    price: float

@app.post("/items")
async def create_item(item: Item):  # FastAPI 自动解析 JSON → Item
    return item

# 3. 依赖注入
from fastapi import Depends

def get_db():                       # 依赖函数
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()

@app.get("/users")
def list_users(db: Session = Depends(get_db)):  # 注入数据库连接
    return db.query(User).all()

# 4. 中间件
app.add_middleware(CORSMiddleware, allow_origins=["*"])
```

**项目中哪里用到：** `src/main.py` 创建 app，12 个 router 各司其职。

---

### 2.2 LangChain 文档处理

**是什么：** LLM 应用开发框架，提供文档加载、文本分割、链式调用等工具。

**项目只用了它的两个组件：**

#### RecursiveCharacterTextSplitter（文本分割器）

```python
from langchain_text_splitters import RecursiveCharacterTextSplitter

splitter = RecursiveCharacterTextSplitter(
    chunk_size=1000,        # 每个 chunk 最大字符数
    chunk_overlap=200,      # 相邻 chunk 重叠字符数
    separators=["\n\n", "\n", "。", "！", "？", ".", " ", ""]
    # 分割优先级：段落 → 换行 → 中文句号 → 英文句号 → 空格 → 硬切
)

chunks = splitter.split_text(long_document)
# "Python是...编程语言。\n\nFastAPI是..." → ["Python是...编程语言。", "FastAPI是..."]
```

**关键参数选择：**
- `chunk_size=1000`：太小语义不完整，太大检索不精确。500-1500 是经验范围
- `chunk_overlap=200`：防止关键信息正好被切断在 chunk 边界
- 中文分隔符 `。！？` 比英文的 `.` 更适合中文文档

---

### 2.3 Embedding 文本向量化

**是什么：** 把文本转成固定维度的数字向量。语义相近的文本，向量在高维空间中距离近。

**类比：** 把"苹果"和"水果"放在坐标轴上的近处，把"苹果"和"汽车"放在远处。

**项目支持的模型：**

| 模型 | 维度 | 语言 | 大小 | 适用场景 |
|------|------|------|------|---------|
| BAAI/bge-small-zh-v1.5 | 512 | 中英双语 | ~100MB | **默认，中文效果好** |
| all-MiniLM-L6-v2 | 384 | 英语 | ~80MB | 英文文档，速度快 |
| text-embedding-v1 (DashScope) | 1536 | 中英 | 云端 | 企业级，效果最好 |

**代码解析：**

```python
# sentence_embedding.py
class SentenceTransformersEmbedding:
    def embed_query(self, text: str) -> List[float]:
        # 输入: "Python是什么"
        # 输出: [0.01, -0.03, 0.08, ...]  # 512个浮点数
        embedding = self.model.encode(text, normalize_embeddings=True)
        return embedding.tolist()

    def embed_documents(self, texts: List[str]) -> List[List[float]]:
        # 输入: ["文档1", "文档2", "文档3"]
        # 输出: [[512维向量], [512维向量], [512维向量]]
        embeddings = self.model.encode(texts, normalize_embeddings=True)
        return embeddings.tolist()
```

**normalize_embeddings=True 的作用：** 将向量归一化到单位长度。归一化后，两点内积 = 余弦相似度，计算更快。

---

### 2.4 ChromaDB 向量数据库

**是什么：** 嵌入式向量数据库，专门存储和检索向量。不需要单独部署服务器。

**为什么选 Chroma 而不是 Milvus/Pinecone：**
- 嵌入式：直接 import 就能用，零部署
- 持久化：数据存磁盘，重启不丢
- API 简洁：Python 原生接口

**核心操作：**

```python
import chromadb

# 1. 创建客户端
client = chromadb.PersistentClient(path="./chroma_db")

# 2. 创建/获取集合
collection = client.get_or_create_collection(
    name="rag_documents",
    metadata={"hnsw:space": "cosine"}  # 余弦相似度
)

# 3. 插入数据
collection.add(
    ids=["doc1_chunk0", "doc1_chunk1"],
    documents=["Python是编程语言", "FastAPI是Web框架"],
    embeddings=[[0.1, 0.2, ...], [0.3, 0.4, ...]],
    metadatas=[{"source": "python.md"}, {"source": "fastapi.md"}]
)

# 4. 检索
results = collection.query(
    query_embeddings=[[0.15, 0.25, ...]],  # 查询向量
    n_results=5,                            # 返回 Top-5
    include=["documents", "metadatas", "distances"]
)
# results["documents"][0] = ["Python是编程语言", ...]
# results["distances"][0] = [0.12, 0.35, ...]  # 越小越相似
```

**项目中哪里用到：** `src/database/vector_db.py` 的 `ChromaStore` 类。

---

### 2.5 混合检索：BM25 + 向量 + RRF

**这是面试最加分的技术点。**

**问题：** 纯向量检索对精确关键词匹配差。搜"Python 3.12"可能返回 Python 2.7 的内容。

**方案：** 两条检索管线并行，结果融合。

#### BM25 关键词检索

```
核心公式：score(D,Q) = Σ IDF(qi) × (f(qi,D) × (k1+1)) / (f(qi,D) + k1×(1-b+b×|D|/avgdl))
```

通俗解释：
- IDF（逆文档频率）：稀有词权重高。"Python"在很多文档出现 → 权重低，"Guido"只在少数文档 → 权重高
- TF（词频）：文档中该词出现次数
- k1, b：调节参数，控制词频饱和度

#### RRF（Reciprocal Rank Fusion）融合

```
RRF_score(d) = Σ 1/(k + rank_i(d))
```

通俗解释：一个文档在两个检索器中排名越靠前，融合分数越高。k=60 是经验值，避免第1名和第2名差距过大。

**代码位置：** `src/retrieval/hybrid_retriever.py`

**项目中的开关：** `.env` 中 `ENABLE_HYBRID_SEARCH=true`

---

### 2.6 CrossEncoder 重排序

**是什么：** 对粗排结果进行第二遍精排。粗排快但不准（Bi-encoder），精排慢但准（Cross-encoder）。

**Bi-encoder vs Cross-encoder：**

| | Bi-encoder | Cross-encoder |
|------|-----------|--------------|
| 编码方式 | query 和 doc 分别编码 | query 和 doc 一起编码 |
| 速度 | 快（可以预计算 doc 向量） | 慢（每对都要重新算） |
| 精度 | 较低 | 高 |
| 用途 | 粗排（从万到百） | 精排（从百到五） |

**为什么两阶段：** 全量用 Cross-encoder 太慢（每次都要算 query-doc pair），所以先用 Bi-encoder 粗排取 Top-20，再用 Cross-encoder 精排取 Top-5。

**代码位置：** `src/retrieval/reranker.py`

---

### 2.7 LLM 多模型客户端

**设计模式：** 策略模式 — 同一接口，不同实现。

```python
# llm_client.py 核心结构
class LLMClient:
    def __init__(self):
        self.provider = settings.LLM_PROVIDER  # "ollama" | "deepseek" | "dashscope"

    def generate_response(self, query, context):
        prompt = f"基于以下上下文...\n{context}\n问题：{query}"
        return self._call(prompt)

    def _call(self, prompt):
        if self.provider == "ollama":
            return self._generate_ollama(prompt)     # localhost:11434
        elif self.provider == "deepseek":
            return self._generate_deepseek(prompt)   # api.deepseek.com
        elif self.provider == "dashscope":
            return self._generate_dashscope(prompt)  # dashscope.aliyuncs.com
```

**三种模型的区别：**

| | Ollama | DeepSeek | DashScope (Qwen) |
|------|--------|----------|-----------------|
| 部署 | 本地 | 云端 | 云端 |
| 费用 | 免费 | ~1元/千次 | ~2元/千次 |
| 速度 | 看配置 | 快 | 快 |
| 中文 | 中 | 强 | 很强 |

**SSE 流式生成：**

```python
def generate_stream(self, prompt):
    # DeepSeek 流式
    for line in resp.iter_lines():
        if line.startswith(b"data: "):
            chunk = json.loads(line[6:])
            yield chunk["choices"][0]["delta"].get("content", "")
    # Ollama 流式
    for line in resp.iter_lines():
        yield json.loads(line).get("response", "")
```

**Prompt 模板（RAG 用）：**

```
基于以下上下文信息，回答问题。

上下文：
[1] Python是一种高级编程语言，由Guido van Rossum创建...
[2] Python的第一个版本发布于1991年...

问题：Python是谁创建的？

要求：
1. 基于上下文回答，不添加外部知识
2. 如上下文无相关信息，明确说明"根据提供的信息无法回答"
3. 引用相关段落编号
```

---

### 2.8 SSE（Server-Sent Events）流式输出

**是什么：** 服务器向客户端持续推送数据的技术。单向（服务器→客户端），基于 HTTP。

**对比 WebSocket：**
- SSE：单向、简单、基于 HTTP、自动重连
- WebSocket：双向、复杂、需要升级协议

**为什么用 SSE 做流式输出：** LLM 是逐 token 生成的，SSE 天然适合"一个接一个推送"的场景。

**实现要点：**

```python
from fastapi.responses import StreamingResponse

@router.post("/stream")
def chat_stream(request: ChatRequest):
    def generate():
        # 1. 先推送检索来源
        yield f"event: sources\ndata: {sources_json}\n\n"
        # 2. 逐 token 推送生成的回答
        for token in llm_client.generate_stream(prompt):
            yield f"data: {json.dumps({'token': token})}\n\n"
        yield "data: [DONE]\n\n"

    return StreamingResponse(
        generate(),
        media_type="text/event-stream",
        headers={"Cache-Control": "no-cache"}
    )
```

**代码位置：** `src/api/routers/chat.py` 的 `/stream` 端点。

---

### 2.9 SQLAlchemy + SQLite

**是什么：** SQLAlchemy 是 Python 最流行的 ORM（对象关系映射）。SQLite 是嵌入式关系数据库。

**为什么用 SQLite：** 零部署，单文件存储，适合开发和小规模部署。生产可切换 PostgreSQL。

**模型定义示例：**

```python
# models.py
class KnowledgeBase(Base):
    __tablename__ = "knowledge_bases"
    id = Column(Integer, primary_key=True)
    name = Column(String, nullable=False)
    owner_id = Column(Integer, ForeignKey("users.id"))
    documents = relationship("KnowledgeDocument", back_populates="knowledge_base")

class KnowledgeDocument(Base):
    __tablename__ = "knowledge_documents"
    id = Column(Integer, primary_key=True)
    kb_id = Column(Integer, ForeignKey("knowledge_bases.id"))
    filename = Column(String, nullable=False)
    status = Column(Integer, default=0)  # 0=Uploading, 1=Processing, 2=Completed, 3=Failed
```

**13 张表：** User, KnowledgeBase, KnowledgeDocument, DocumentChunk, GeneratedQAPair, ChatSession, ChatInteraction, EvaluationTask, EvaluationResult, EvaluationDatasetItem, Assistant, Agent

---

### 2.10 JWT 认证

**是什么：** JSON Web Token。用户登录后服务器签发 Token，后续请求携带 Token 验证身份。

**流程：**

```
1. 用户注册 → POST /auth/register → 密码哈希存入 SQLite
2. 用户登录 → POST /auth/login/access-token → 验证密码 → 签发 JWT
3. 后续请求 → Header: Authorization: Bearer <token> → 验证 JWT → 获取用户信息
```

**关键代码：**

```python
# dependencies.py
def get_current_user(token: str = Depends(oauth2_scheme)):
    payload = jwt.decode(token, settings.SECRET_KEY, algorithms=["HS256"])
    user_id = payload.get("sub")
    user = db.query(User).filter(User.id == int(user_id)).first()
    return user
```

---

### 2.11 Streamlit 前端

**是什么：** 纯 Python 写 Web UI 的框架。不需要学 HTML/CSS/JavaScript。

**为什么不用 Vue 3：** Vue 3 需要 Node.js 生态，学习成本高。Streamlit 一行 Python = 一个 UI 组件。

**核心组件：**

```python
import streamlit as st

st.title("标题")
st.text_input("输入框")
st.file_uploader("上传文件")
st.button("按钮")
st.spinner("加载中...")     # 加载动画
st.success("成功消息")
st.error("错误消息")
st.markdown("支持 Markdown")
```

**代码位置：** `streamlit_app.py`（216 行，三个页面：问答/文档管理/系统状态）。

---

### 2.12 Docker 容器化

**三个核心概念：**

| 概念 | 类比 | 命令 |
|------|------|------|
| Dockerfile | 菜谱（描述怎么做） | `docker build -f Dockerfile .` |
| Image | 做好的菜的照片（不可变） | `docker images` |
| Container | 正在吃的菜（运行实例） | `docker run` / `docker-compose up` |

**轻量模式（单容器）：**

```yaml
# docker-compose.light.yml
services:
  app:
    build:
      context: ../
      dockerfile: docker/Dockerfile.light
    ports:
      - "8000:8000"
    environment:
      - VECTOR_STORE=chroma
      - LLM_PROVIDER=deepseek
```

启动：`docker-compose -f docker/docker-compose.light.yml up -d`

---

## 三、项目核心链路逐行解析

### 3.1 RAG 问答链路 (rag_service.py)

```python
def query(self, query_text, top_k, session_id, kb_ids, assistant_config):
    # ── 第 0 步：获取对话历史 ──
    memory_config = (assistant_config.get("memory_config") or {}) if assistant_config else {}
    history = self.memory.get_short_term_memory(session_id)  # 短期记忆
    history_str = "\n".join([f"{m['role']}: {m['content']}" for m in history])

    # ── 第 1 步：如果没有 KB，走通用对话模式 ──
    if not kb_ids:
        # 直接调 LLM，不做检索
        answer = self.llm_client.generate_general_response(query_text, context)
        self.memory.add_short_term_memory(session_id, "user", query_text)
        self.memory.add_short_term_memory(session_id, "assistant", answer)
        return {"query": query_text, "answer": answer, "source_documents": []}

    # ── 第 2 步：问题分析（是否多跳）──
    analysis = self.analyzer.analyze(contextual_query)
    # analysis = {"is_multi_hop": False, "sub_queries": [], "reason": "..."}

    # ── 第 3 步：选择检索策略 ──
    if settings.ENABLE_MULTI_HOP and analysis.get("is_multi_hop"):
        result = self._multi_hop_query(...)  # 多跳：拆成子问题，逐步检索
    else:
        result = self._single_hop_query(...)  # 单跳：直接检索

    # ── 第 4 步：更新记忆 ──
    self.memory.add_short_term_memory(session_id, "user", query_text)
    self.memory.add_short_term_memory(session_id, "assistant", result["answer"])
    return result

def _single_hop_query(self, query_text, top_k, history_str, kb_ids, system_prompt):
    # 1. 检索（混合或纯向量）
    initial_k = top_k * 2  # 为重排序多取一些
    retriever = self._get_retriever()  # HybridRetriever or VectorRetriever
    search_results = retriever.retrieve(query_text, top_k=initial_k, kb_ids=kb_ids)

    # 2. 重排序（如果开启）
    if settings.ENABLE_RERANK:
        search_results = self.reranker.rerank(query_text, search_results)
    else:
        search_results = search_results[:top_k]

    # 3. 组装上下文
    context = self._format_context(search_results)  # "段落1: ...\n\n段落2: ..."

    # 4. 调用 LLM 生成
    answer = self.llm_client.generate_response(query_text, context)

    # 5. 返回结果（含引用来源）
    return self._format_response(query_text, answer, search_results)
```

### 3.2 混合检索链路 (hybrid_retriever.py)

```python
def retrieve(self, query, top_k, kb_ids, alpha=0.5):
    fetch_k = top_k * 3  # 多取一些给 RRF 融合

    # 管道 1：向量检索
    vector_results = self.vector_retriever.retrieve(query, top_k=fetch_k, kb_ids=kb_ids)

    # 管道 2：BM25 检索
    bm25_results = self.bm25_retriever.search(query, top_k=fetch_k)

    # RRF 融合
    rrf_scores = {}
    for rank, result in enumerate(vector_results):
        rrf_scores[result.text[:200]] = {"score": 1/(60+rank+1)*alpha, "result": result}
    for rank, result in enumerate(bm25_results):
        key = result.text[:200]
        score = 1/(60+rank+1)*(1-alpha)
        if key in rrf_scores:
            rrf_scores[key]["score"] += score  # 在两个管道都出现的文档得分更高
        else:
            rrf_scores[key] = {"score": score, "result": result}

    # 按融合分数排序
    fused = sorted(rrf_scores.values(), key=lambda x: x["score"], reverse=True)
    return [item["result"] for item in fused[:top_k]]
```

### 3.3 BM25 关键词检索

```python
class BM25Retriever:
    def __init__(self, k1=1.5, b=0.75):
        self.k1 = k1  # 词频饱和度参数
        self.b = b    # 文档长度归一化参数

    def index_documents(self, documents, metadatas):
        # 1. 计算平均文档长度
        self.avgdl = sum(len(d) for d in documents) / len(documents)

        # 2. 统计每个词的文档频率
        for doc in documents:
            terms = self._tokenize(doc)
            for term in set(terms):
                self.df[term] += 1

        # 3. 计算 IDF
        self.idf[term] = log(1 + (N - df + 0.5) / (df + 0.5))

    def _tokenize(self, text):
        # 中英文混合分词
        # 英文: "python programming" → ["python", "programming"]
        # 中文: "机器学习" → ["机", "器", "学", "习"]

    def search(self, query, top_k):
        # 对每个文档计算 BM25 分数
        for i, doc in enumerate(self.documents):
            score = 0
            for term in query_terms:
                tf = self.term_freqs[i].get(term, 0)
                idf = self.idf.get(term, 0)
                score += idf * tf * (self.k1+1) / (tf + self.k1*(1-self.b+self.b*len(doc)/self.avgdl))
        # 返回分数最高的 top_k 个文档
```

### 3.4 文档上传 + 同步处理链路

```python
# knowledge_base.py: _process_document_async (轻量模式同步版本)
def _process_document_async(doc_id):
    db = SessionLocal()
    doc = db.query(KnowledgeDocument).filter(id == doc_id).first()
    doc.status = 1  # Processing

    # 1. 解析文档
    parser = MultiDocParser()
    parsed = parser.parse(doc.file_path)  # 自动检测 PDF/DOCX/MD/TXT

    # 2. 分割
    chunker = TextChunker(chunk_size=1000, chunk_overlap=200)
    chunks = chunker.split_document(parsed)  # → List[Chunk]

    # 3. Embedding
    emb_svc = get_embedding_service()
    embeddings = emb_svc.embed_documents([c.text for c in chunks])

    # 4. 存入 Chroma
    store = get_vector_store()
    records = [VectorRecord(id=uuid4(), values=emb, metadata={...}) for ...]
    store.insert(records)

    # 5. 更新状态
    doc.status = 2  # Completed
    doc.chunk_count = len(chunks)
    db.commit()
```

---

## 四、综合练习题

> 先自己做，再对答案。每题都要能用自己的话讲出来。

### 基础题（1-10）

**Q1.** 画出 RAG 的完整流程图，标注每一步用到的具体技术和组件。

**Q2.** 解释 `RecursiveCharacterTextSplitter` 的工作原理。`chunk_size=1000, overlap=200` 是什么意思？

**Q3.** Embedding 向量为什么能表示语义？两个向量的余弦相似度 0.95 和 0.2 分别代表什么？

**Q4.** ChromaDB 的 `collection.add()` 和 `collection.query()` 分别接受什么参数？返回什么？

**Q5.** 在项目中，一个文档从上传到可以被问答，经过了哪些步骤？列出每一步的输入和输出。

**Q6.** JWT 认证的流程是什么？Token 里面存了什么？为什么不需要每次请求都查数据库验证密码？

**Q7.** FastAPI 的 `Depends` 是做什么的？举一个项目中的例子。

**Q8.** Pydantic `BaseModel` 和 `BaseSettings` 的区别是什么？项目中各用在哪里？

**Q9.** SQLAlchemy 的 `relationship()` 是做什么的？`back_populates` 参数的含义？

**Q10.** `.env` 文件和 `settings.py` 的关系是什么？修改 `.env` 后需要重启服务器吗？

### 进阶题（11-20）

**Q11.** 纯向量检索和纯 BM25 检索各有什么优缺点？试举一个具体例子说明。

**Q12.** RRF 融合公式 `score = Σ 1/(k+rank_i)` 中 k=60 的作用是什么？如果 k=0 会怎样？

**Q13.** Bi-encoder 和 Cross-encoder 的根本区别是什么？为什么项目用两阶段（先粗排后精排）？

**Q14.** 阅读 `hybrid_retriever.py` 的 `retrieve()` 方法。如果 `alpha=0` 或 `alpha=1`，会发生什么？

**Q15.** 阅读 `llm_client.py`。如果要添加一个新的 LLM 提供商（如 OpenAI），需要改哪些地方？

**Q16.** SSE 流式输出和普通 HTTP 请求的区别是什么？`text/event-stream` 这个 Content-Type 告诉浏览器什么？

**Q17.** `rag_service.py` 中的单例模式 `get_rag_service()` 为什么要这样设计？如果每个请求都 new 一个 RAGService 会有什么问题？

**Q18.** 项目中 `memory_service.py` 支持 Redis 和内存字典两种后端。这种"优雅降级"的设计模式叫什么？有什么好处？

**Q19.** 如果文档上传后 status=3 (Failed)，你从哪几个地方排查问题？

**Q20.** 阅读 `settings.py`。如果要关闭混合检索、切换到 Ollama、禁用重排序，需要改 `.env` 中的哪几行？

### 实战题（21-25）

**Q21.** 写一段代码：给定一个文本文件，用项目中的 `TextChunker` 分割成 chunks，用 `get_embedding_service()` 向量化，用 `get_vector_store()` 存入 Chroma。

**Q22.** 修改 `demo.py`：添加一个自定义文档（比如你的简历），然后针对这个文档问 3 个问题并打印回答。

**Q23.** 在 `hybrid_retriever.py` 中添加一个方法 `compare()`，对同一个查询分别输出纯向量、纯 BM25、混合检索的结果，方便对比效果。

**Q24.** 写一个 Python 脚本，调用 `POST /api/v1/chat/stream`，实时打印流式输出的 token。

**Q25.** 写一个 `test_hybrid.py`，测试混合检索在精确关键词匹配上是否优于纯向量检索。

### 设计题（26-30）

**Q26.** 如果要支持多用户、多知识库的权限隔离，数据库表应该怎么设计？画出 ER 图。

**Q27.** 项目目前文档处理是同步的。如果要改成异步（大文件不阻塞请求），你会怎么设计？

**Q28.** 如果要给项目加一个"相似文档推荐"功能（上传文档后推荐知识库里已有的相关文档），你会怎么实现？

**Q29.** 如果要部署到生产环境，你认为当前项目还缺什么？（至少列出 5 项）

**Q30.** 给这个项目的 RAG 效果设计一个评估方案。需要哪些指标？怎么获取 ground truth？

---

## 五、面试模拟题

> 每题先练"一句话回答"，再展开 30 秒。对着镜子练。

### RAG 专精（1-12）

**Q1.** RAG 是什么？解决什么问题？
**Q2.** RAG 的完整流程，从文档上传到回答生成，每一步具体做什么？
**Q3.** Embedding 为什么能表示语义？不同 Embedding 模型有什么区别？
**Q4.** 向量数据库和传统数据库的本质区别？为什么 RAG 用向量数据库？
**Q5.** 文本分割策略怎么选？chunk_size 和 chunk_overlap 设多少合适？
**Q6.** 混合检索是什么？为什么比纯向量检索好？
**Q7.** RRF 融合的原理？为什么不用简单的加权平均？
**Q8.** Bi-encoder 和 Cross-encoder 区别？为什么用两阶段检索？
**Q9.** RAG 和微调的区别？什么场景选哪个？
**Q10.** 怎么评估 RAG 效果？用什么指标？怎么获取测试集？
**Q11.** 怎么防止 LLM 在 RAG 中编造答案（幻觉）？
**Q12.** 你做的 RAG 项目，检索效果不好你会从哪些方面排查？

### FastAPI & 后端（13-19）

**Q13.** FastAPI 和 Flask 的区别？为什么选 FastAPI？
**Q14.** async/await 解决什么问题？asyncio.gather 的用法？
**Q15.** FastAPI 的依赖注入（Depends）是什么？项目中怎么用的？
**Q16.** Pydantic 在 FastAPI 中扮演什么角色？
**Q17.** JWT 认证的原理？和 Session 认证的区别？
**Q18.** CORS 是什么？为什么 FastAPI 需要配置 CORS 中间件？
**Q19.** SSE 的原理？和 WebSocket 的区别？

### 工程能力（20-25）

**Q20.** 你的项目怎么保证代码质量？（测试、类型检查、日志）
**Q21.** SQLAlchemy ORM 的优点？什么情况下你会写原生 SQL？
**Q22.** Docker 解决什么问题？Dockerfile 和 docker-compose.yml 的关系？
**Q23.** 单例模式在项目中的使用？为什么 Embedding/VectorStore 要单例？
**Q24.** 项目中"优雅降级"的设计体现在哪里？为什么这样设计？
**Q25.** 如果要支持水平扩展（多个服务器实例），当前项目有什么限制？

### 项目讲述（26-30）

**Q26.** 用 3 分钟介绍你的 RAG 项目。（按模板练）
**Q27.** 你项目最大的技术亮点是什么？为什么？
**Q28.** 项目中遇到过什么技术难点？怎么解决的？
**Q29.** 如果重新做这个项目，你会怎么改进？
**Q30.** 你的项目和 GitHub 上其他 RAG 项目比有什么不同？

### 场景题（31-40）

**Q31.** 用户反馈"搜'Python性能优化'返回的都是Python基础教程"，你怎么排查和修复？
**Q32.** LLM 生成的回答引用了错误的文档段落，可能的原因和解决方案？
**Q33.** 文档上传后处理失败（status=3），你会怎么排查？
**Q34.** 服务响应很慢（>5秒），可能的原因有哪些？怎么定位？
**Q35.** 用户上传了一个 200MB 的 PDF，系统卡住了。你会怎么处理？
**Q36.** 如果要支持实时更新的知识库（文档修改后立即生效），需要改什么？
**Q37.** 老板说"我们的 RAG 效果不好"，你怎么量化证明效果到底好不好？
**Q38.** 同事想给你的项目加一个新功能，他应该从哪里开始读代码？
**Q39.** 如果 API 请求量突然增加 10 倍，系统哪些地方会先崩？
**Q40.** 你要招一个人接手这个项目，你会考他什么问题？

---

## 六、参考答案

### 基础题

**A1.** RAG 流程图：

```
离线索引阶段：
文档上传 → 格式检测 → 内容解析(PyMuPDF/python-docx)
→ 文本清洗 → RecursiveCharacterTextSplitter 分割
→ SentenceTransformersEmbedding 向量化(512维)
→ ChromaDB 存储{id, embedding, text, metadata}

在线查询阶段：
用户问题 → JWT 认证 → 问题分析(是否多跳)
→ (可选)查询改写 → Hybrid Retriever
  ├─ BM25 关键词检索 (精确匹配)
  └─ 向量语义检索 (语义相似) → RRF 融合
→ CrossEncoder 重排序 Top-5
→ 拼接 RAG Prompt → LLM 生成
→ 保存对话记录 → 返回{answer, sources}
```

**A2.** `RecursiveCharacterTextSplitter` 按分隔符优先级递归切割。`chunk_size=1000` 是每个 chunk 最大字符数，`overlap=200` 是相邻 chunk 重叠的字符数。举例："ABCDEFGHIJ" 按 size=5, overlap=2 分割 → ["ABCDE", "DEFGH", "GHIJ"]。

**A3.** Embedding 模型通过对比学习训练：让相似文本的向量距离近，不相似文本的距离远。余弦相似度 0.95 = 几乎相同含义，0.2 = 基本无关。

**A4.** `add(ids, documents, embeddings, metadatas)` 批量插入。`query(query_embeddings, n_results, include)` 返回最相似的 n 个结果。

**A5.** 上传(字节流) → 保存文件 → MultiDocParser.parse(文件路径) → Document对象(含content) → TextChunker.split_document(Document) → List[Chunk] → EmbeddingService.embed_documents(texts) → List[vectors] → ChromaStore.insert(VectorRecords) → status=2。

**A6.** 用户登录→服务器验证密码→签发JWT(含user_id+过期时间+签名)→客户端存储Token→后续请求带`Authorization: Bearer <token>`→服务器验签→获取user_id。不需要查数据库是因为JWT签名可以防篡改。

**A7.** `Depends` 是 FastAPI 的依赖注入机制。例如 `def get_current_user(token = Depends(oauth2_scheme))` 自动从请求头提取 Token 并验证。

**A8.** `BaseModel` 定义数据模型（请求体/响应体），`BaseSettings` 额外支持从环境变量和 `.env` 文件读取配置。

**A9.** `relationship()` 定义 ORM 对象间的关联，`back_populates` 指定反向引用的属性名。如 `KnowledgeBase.documents` 和 `KnowledgeDocument.knowledge_base` 互指。

**A10.** `settings.py` 定义配置字段和默认值，`.env` 覆盖默认值。修改 `.env` 需要重启服务器（或用 `--reload` 自动重启）。

### 进阶题

**A11.** 纯向量："如何学习编程"能匹配到"Python入门教程"（语义相近）。纯 BM25："Python 3.12"精确匹配到含该字符串的文档。向量不适合精确术语搜索，BM25 不适合语义搜索。

**A12.** k=60 平滑高排名和低排名的差距。k=0 时第 1 名得分 1.0，第 2 名 0.5，差距过大不稳定。

**A13.** Bi-encoder 分开编码（query 和 doc 独立过模型），可以预计算 doc 向量，检索时只算 query。Cross-encoder 联合编码（query+doc 一起过模型），每对都要重新算。两阶段=粗排快+精排准。

**A14.** `alpha=0` 只用 BM25（纯关键词），`alpha=1` 只用向量（纯语义）。默认 0.5 等权融合。

**A15.** 在 `_check_availability` 中添加 `"openai"` 分支；实现 `_generate_openai` 方法；在 `_call` 中添加路由；在 `settings.py` 添加 `OPENAI_API_KEY`。

**A16.** 普通 HTTP 请求：客户端发请求→服务器处理完→一次返回全部数据。SSE：客户端发请求→服务器保持连接→逐条推送数据→`[DONE]` 结束。`text/event-stream` 告诉浏览器这是 SSE 流。

**A17.** 单例避免重复加载模型（Embedding 500MB、CrossEncoder 500MB）。每次 new 会 OOM 且启动极慢。

**A18.** 策略模式 + 降级。好处：开发环境零依赖，生产环境高性能，同一套代码。

**A19.** ①查 SQLite `SELECT error_msg FROM knowledge_documents WHERE id=N`；②查服务器日志；③检查文件是否存在且可读；④手动跑 `MultiDocParser().parse(filepath)` 测试。

**A20.** `.env` 中改三行：`ENABLE_HYBRID_SEARCH=false`、`LLM_PROVIDER=ollama`、`RERANK_PROVIDER=none`。

### 实战题（要点）

**A21.** 参考 `demo.py` 的 Step 2 部分代码。

**A22.** 在 `sample_docs` 字典中添加你的简历内容，运行 `python demo.py`。

**A23.** 在 `HybridRetriever` 中添加方法，分别调用 `self.bm25_retriever.search()` 和 `self.vector_retriever.retrieve()` 以及 `self.retrieve()`，打印三份结果。

**A24.** 用 `requests.get(stream=True)` + `iter_lines()` 逐行解析 SSE 事件。

**A25.** 准备一个包含专有名词的文档（如产品型号），分别用纯向量和混合检索查询，比较结果。

### 设计题（要点）

**A26.** User 1:N KnowledgeBase, KnowledgeBase 1:N Document, Document 1:N Chunk。中间加一个 UserKB 关联表实现权限控制。

**A27.** 用 Celery + RabbitMQ/Redis 做异步任务队列。上传后返回 task_id，轮询任务状态。

**A28.** 对上传文档做 Embedding → 与知识库中所有文档的摘要向量做相似度计算 → 返回 Top-5 相似文档。

**A29.** ①HTTPS ②日志收集(ELK) ③监控(Prometheus) ④限流(Rate Limiting) ⑤数据库迁移(Alembic) ⑥CI/CD ⑦单元测试覆盖率>80%。

**A30.** 指标：Faithfulness（答案是否忠于上下文）、Answer Relevance（答案是否相关）、Context Precision（检索精度）。Ground truth：人工标注 50-100 个问答对。

### 面试模拟题（要点）

**A1.** RAG = Retrieval-Augmented Generation，检索增强生成。解决 LLM 知识过时和幻觉问题。类比：开卷考试 vs 闭卷考试。

**A3.** 对比学习训练。BGE 中文好，all-MiniLM 英文好快，OpenAI text-embedding-3 效果最好但要钱。

**A6.** BM25 精确关键词 + 向量语义相似 + RRF 融合。搜"Python 3.12 新特性"时，BM25 确保返回含"3.12"的文档，向量确保返回相关的版本更新文档。

**A8.** Cross-encoder 把 query 和 doc 拼一起给模型打分，准确但慢。所以先用 Bi-encoder 粗排取 20 个，再用 Cross-encoder 精排取 5 个。

**A11.** 三层防护：①Prompt 中写死"如无信息请说不知道"；②每个回答标注引用来源；③RAGAS Faithfulness 自动检测。

**A12.** 从检索→重排→生成逐层排查：查检索是否返回了相关文档（可能向量化有问题），查重排序是否排对了（可能模型不合适），查 Prompt 是否合理（可能上下文太长被截断）。

**A14.** IO 密集场景不阻塞。`gather(task1, task2, task3)` 并发执行三个协程，等全部完成返回结果列表。

**A20.** pytest 12 项测试，12/12 通过。类型注解覆盖所有函数。Loguru 结构化日志。

**A26. 3分钟模板见面试背诵卡.md。**

**A31.** 诊断：先用测试查询看检索结果，可能向量检索返回了语义相关但不精确的结果 → 开启混合检索，BM25 提高精准关键词权重。

**A35.** 大文件分片上传 + 异步处理 + 进度回调。前端显示"正在处理 45%..."

**A39.** ①LLM API 限流 ②向量库检索瓶颈 ③SQLite 写锁 ④内存不足。方案：LLM 加队列+缓存，Chroma 换 Milvus 分布式，SQLite 换 PostgreSQL。

---

> **学习建议：** 先对着"项目学习指南.md"理解架构，再回来做练习题。每天 5 道题，一周刷完。面试前把"面试背诵卡.md"的 25 题对着镜子练 3 遍。
